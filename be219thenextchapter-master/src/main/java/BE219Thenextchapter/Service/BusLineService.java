package BE219Thenextchapter.Service;

public interface BusLineService {

    //Read
   /* List<BusRouteDTO> getAll();
    BusRouteDTO getBusLineById(long id);

    //Create
    BusLineDTO addBusLine(BusRouteDTO busRouteDTO) ; //bekommt DTO wird mit Mapper in Objekt umgew. und gespeichert

    //Update
    BusLineDTO updateBusLine(BusLineDTO busLineDTO);

    //Delete
    void removeBusLineById(long id) ; */
}
